# Uncomment the next line to define a global platform for your project

platform :ios, '12.0'
use_frameworks!

target 'iOS-step-tracker' do
  pod 'SwiftyBeaver'
  pod 'Firebase/Core'
  pod 'Firebase/Database'

end
